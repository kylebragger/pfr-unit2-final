<%= form_for(@user, html: { style: 'background: white; padding: 20px; border: 3px solid #ddd' }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <div>
        <%= f.label :email %><br>
        <%= f.email_field :email %>
    </div>

    <div>
        <%= f.label :username %><br>
        <%= f.text_field :username %>
    </div>

    <% if @user.new_record? %>
    <div>
        <%= f.label :password, 'Password' %><br>
        <%= f.password_field :password %>
    </div>
    <% end %>
    
    Profile:
    
    <div>
        <%= f.label :profile_bg, 'Background' %><br>
        #<%= f.text_field :profile_bg %>
    </div>
    
    <div>
        <%= f.label :profile_fg, 'Text' %><br>
        #<%= f.text_field :profile_fg %>
    </div>
    
    <div>
        <%= f.label :profile_image_file, 'Cover Image' %><br>
        <%= f.file_field :profile_image_file %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
